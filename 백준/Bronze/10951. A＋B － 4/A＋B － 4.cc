#include<bits/stdc++.h>
using namespace std;

int main() {
    while(1) {
        int a,b;
        cin >> a >> b;
        if(cin.eof() == 1) {
            break;
        } else {
            cout << a+b << "\n";
        }
    }
}